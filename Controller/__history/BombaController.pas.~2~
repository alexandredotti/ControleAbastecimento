unit BombaController;

interface

uses Bomba, BombaDAO, SysUtils;

type
  TBombaController = class
  private
    { private declarations }
  public
    procedure Salvar(aBomba: TBomba);
    procedure Excluir(aBomba: TBomba);
  end;

implementation

{ TTanqueBO }

procedure TBombaController.Excluir(aBomba: TBomba);
var
  BombaDAO: TBombaDAO;
begin
  try
    BombaDAO := TBombaDAO.Create;
    BombaDAO.Excluir(aBomba);
    FreeAndNil(BombaDAO);
  except on E: Exception do
    begin
      raise Exception.Create(E.Message);
    end;
  end;
end;

procedure TBombaController.Salvar(aBomba: TBomba);
var
  BombaDAO: TBombaDAO;
begin
  try
    BombaDAO := TBombaDAO.Create;
    BombaDAO.Salvar(aBomba);
    FreeAndNil(BombaDAO);
  except on E: Exception do
    begin
      raise Exception.Create(E.Message);
    end;
  end;
end;

end.
